query GetEvents($pagination: Pagination) {
  GetEvents(pagination: $pagination) {
    total
    events {
      id
      name
      description
      image
      created_at
      start_at
      end_at
    }
  }
}
